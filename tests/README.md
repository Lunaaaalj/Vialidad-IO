# Tests Directory

This directory contains unit tests and integration tests for the project.

## Guidelines

- Write tests for all functions in the `src/` directory
- Use a testing framework like pytest or unittest
- Name test files with the prefix `test_`
- Aim for high test coverage
- Run tests regularly during development
- Include tests in CI/CD pipeline
